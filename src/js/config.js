//So here in this file we will use the all the constants and variables used in other will be declared and you can reused it

//We will not put all the variables but the one which is important across the project

//using uppercase is used for a constants that will never change.its a commomn practice specially in a configuration file like this 👇🏽😀
export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';
export const TIMEOUT_SEC = 10;
export const RESULT_PER_PAGE = 10;
